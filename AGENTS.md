# Tunta 设计系统约定

给改这个仓库 UI 的人（和 agent）看。数值都在 `src/styles/tokens.css`，这里只写**什么时候用哪个**。

## 配色

调色板只有四组东西：沙/纸、棕、蓝、黄。它们不是四个可选装饰色，而是四个角色。
判断一个颜色用得对不对，问一句：**这块颜色在回答什么问题？**

| 角色 | 色 | 回答的问题 | token |
|---|---|---|---|
| 地 | 沙 / 纸 | 「内容住在哪」 | `--brand-sand` `--paper-bright` `--surface-sunken` |
| 字 | 棕 otter | 「这是什么」 | `--ink` `--muted` `--fill-neutral*` |
| 位 | 蓝 water | 「我在哪 / 去哪」 | `--brand-water*` |
| 焦 | 黄 sun | 「此刻该动哪一处」 | `--brand-sun` `--brand-sun-pale` |

### 棕是默认值

**拿不准就用棕。** 中性控件填充、标签、表格底板、hover，全部走 otter 的低透明度叠加
（`--fill-neutral` 系列、`--surface-sunken`）。半透明是有意的：同一个 token 压在沙面和纸面上
都会自动比底色深一档，不用为每种底色各配一套色值。

一个颜色只有在能回答「位」或「焦」的问题时，才有资格不是棕。

### 蓝标位置，不标内容

蓝色属于导航与品牌：侧边栏、页面 hero、链接、证据条（去原文的入口）、
筛选器的当前选中项、引文序号与正文里对应的 ① 标记。

蓝色**不用于**：内容分类标签、表格/列表底板、遮罩、每行都会出现的元数据。
这些东西一屏会出现十几次，蓝色一旦复数就从 brand 掉成了默认背景色。

> **一屏一块大蓝。** 侧边栏常驻不算。如果一个页面同时有蓝 hero 和蓝底板，
> 那块底板就是写错了——改成 `--surface-sunken`。

### 黄只有一处

黄色的意思是「**此刻该看/该动的就是这里**」，所以它天然是单数：

- 当前所在的导航项
- 当前这一步的主操作（顶栏「收藏」、回看页「归档」）
- 一次性的注意力引导（点 ① 跳转时引文块闪一下）

黄色**不用于**：分类标签、列表 hover、每行都有的控件、关闭/取消这类退出动作。
一旦同一屏出现两个含义不同的黄，它就不再是高亮，只是装饰。

判据很简单：**这块黄在当前视口里会不会出现第二次？** 会，就说明它标的不是焦点。

### 收走颜色，就得用明度补上

这套调色板是暖色低对比的，沙 / 纸 / 中性填充天然挤在同一个明度带里。
所以把一处的蓝或黄换成中性之后，**必须检查那块区域还剩多少明度层次**——
否则只是把「花」换成了「糊」，一样难看。

补的办法是层次，不是把品牌色加回去：

- 焦点对象要浮起来，就给它下沉的底（`--surface-sunken`）和更高一档的浮层阴影
- 同一排控件最多两档（中性 / 主操作），切成三档就成噪音了
- 最重的视觉块必须落在最重要的元素上。次要入口做成满底深色，会把标题压住

### 红绿只做语义

`--red` / `--green` 只表示失败与完成，不参与任何装饰性表达。

## 圆角

形状也带语义，别全页药丸。完整阶梯和选档规则写在 `src/styles/tokens.css` 的圆角注释里，
一句话版本：**`--r-pill` 只留给不可点的只读词块**（status-chip / card-type / trace-kind 这类），
交互控件按高度选 `--r-xs`（小 chip）或 `--r-sm`（按钮），容器用 `--r-md` / `--r-lg`，
嵌套时外层 = 内层 + padding。

## 边界与层级

只有两套，不要发明第三套：

- **outline**（`--outline-control` / `--outline-selected` / `--outline-focus`）：边界与状态，无高度。
- **hairline shadow**（`--shadow-card` / `--shadow-floating` / `--shadow-overlay`）：浮层高度。

交互反馈用填充加深（rest → hover → active），不要 hover 抬升、不要位移。

## 控件

- 下拉一律用 `src/shared/components/Select.tsx`，不要写原生 `<select>`：
  原生 select 在 macOS 上会把菜单压在触发器上方，看起来像按钮被盖掉了。
- 状态不能只挂在颜色上。选中态在换底色之外，还要给一个静态线索（字重、✓、文案变化）。
